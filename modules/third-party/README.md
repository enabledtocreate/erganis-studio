# Third-party modules

External Erganis Studio plugins installed here. Each module ships an `erganis.module.yaml` (compiled to JSON at build time).

See [Core module manifest schema](../../core/contracts/schemas/module/README.md).
